/ Program to calculate power using ** operator

function calculatePower(base, exponent) {
  const result = base ** exponent;
${base} ^ ${exonent} = ${result}`);
}

// Exampe usage:
calculatePower(3, 2);  // Output: 3 ^ 2 = 9
